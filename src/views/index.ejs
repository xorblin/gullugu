<link rel="stylesheet" href="/index.css">
<div class="container row">
    <div class=" media-container row">
        <canvas id="canvas" class="streamCanvas"></canvas>
        <div class="userVideoContainer ">
            <video id="video" class="userVideo" src=""></video>
        </div>
    </div>
    <div class="container-item col cent">
        <div class="chat-container">
            <div class="chat-header-container row">
                <h3>Anonymous</h3>
            </div>
        </div>
        <div class="chat-message-container"></div>
        <div class="message-input-container row">
            <input type="text" class="message-input" placeholder="Enter your message..." required />
            <button class="send-btn">Send</button>
        </div>
    </div>
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.7.2/socket.io.js"
    integrity="sha512-zoJXRvW2gC8Z0Xo3lBbao5+AS3g6YWr5ztKqaicua11xHo+AvE1b0lT9ODgrHTmNUxeCw0Ry4BGRYZfXu70weg=="
    crossorigin="anonymous" referrerpolicy="no-referrer"></script>
<script>
    const socket = io();
    socket.on('connect', () => {
        console.log('connected');
    });
</script>

<script>
    let canvas = document.getElementById('canvas');
    let context = canvas.getContext('2d');
    let video = document.getElementById('video');
    if (navigator.mediaDevices && navigator.mediaDevices.getUserMedia) {
        navigator.mediaDevices.getUserMedia({ video: true }).then(function (stream) {
            video.srcObject = stream;
            video.play();
        });
    }

</script>